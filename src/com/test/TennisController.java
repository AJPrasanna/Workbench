package com.test;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
public class TennisController {
	/*
	 * @RequestMapping("/tennis") public String mainMenu(Model model) {
	 * model.addAttribute("tennis", new Tennis()); return "tennis-form"; }
	 * 
	 * @RequestMapping("/tennis-result") public String tennisMenu() { return
	 * "tennis-result"; }
	 */
}
